// This project uses Tailwind CSS v4 via @tailwindcss/vite (not PostCSS).
// This empty config prevents any globally-installed PostCSS plugins
// (e.g., a global Tailwind v3) from being picked up by Vite.
export default {
  plugins: {},
};
